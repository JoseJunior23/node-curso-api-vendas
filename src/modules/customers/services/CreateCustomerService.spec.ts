describe('CreateCustomer', () => {
  it('should be table to create a new customer', () => {
    expect(1).toBe(1);
  });

  it('should no be able to create a two customers with the same email', () => {
    expect(1).toBe(1);
  });
});
